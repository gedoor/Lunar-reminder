<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <Preference
        android:key="@string/pref_key_calendar_name"
        android:title="@string/pref_title_calendar_name"
        android:summary="@string/lunar_calendar_name"/>

    <Preference
        android:key="@string/pref_key_google_account"
        android:title="Account name"/>

    <Preference
        android:key="@string/pref_key_calendar_id"
        android:title="Lunar calendar id"
        android:summary=""/>

    <org.xdty.preference.ColorPreference
        android:key="@string/pref_key_calendar_color"
        android:title="Calendar color"
        tools:dialogTitle="@string/select_color"
        tools:colors="@array/default_rainbow"
        tools:columns="5"
        tools:material="true"
        tools:backwardsOrder="true"
        tools:strokeWidth="0"
        android:defaultValue="@color/yellow"/>

</PreferenceScreen>
